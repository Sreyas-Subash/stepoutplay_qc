# to-do
# spreadsheet api integration
# stepout api endpoints integration
# option to run one or multiple files
# reading files automatically from google form google spreadsheet file
# monitoring analyst performance through error count, 24 hr completion
# validating error correction by analysts after qc
# automatically delete files after 1 week
# make match_renum_details.xlsx as monthwise
# corner previous action check qc
# match id - 1759 match report bug
# when no error, no output -> resolve with some output
# check if both shn and fhn tagged
# check for timestamp continuity
# 1739 error - arvind - check specially

# GD-0 and AD-0 check
# THW-1 showing in successful defensive actions


# import gspread
# import pandas as pd
#
# data = [['manu', 10, 99], ['raj', 12, 65], ['nandu', 11, 88]]
# df = pd.DataFrame(data, columns=['name', 'age', 'score'])
#
# gc = gspread.service_account(filename='D:\stepout\qc\creds.json')
#
# sh = gc.create('api_create')
#
# worksheet = sh.add_worksheet(title="A worksheet", rows=100, cols=20)
#
# worksheet.update([df.columns.values.tolist()] + df.values.tolist())
#
# sh.share('sreyassubash99@gmail.com', perm_type='user', role='writer')


# create a check to the string timestamp is in sequential order
# try except block that even if one match string has issue the code should not break


